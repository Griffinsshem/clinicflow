# --- Web framework ---
Flask>=3.1,<4.0
Flask-SQLAlchemy>=3.1,<4.0
Flask-Migrate>=4.0,<5.0

# --- Auth ---
# Flask-JWT-Extended over hand-rolled PyJWT: token expiry, the
# @jwt_required decorator, and identity claims without us writing
# crypto plumbing. Password hashing uses Werkzeug's scrypt, which
# ships with Flask — no extra dependency.
Flask-JWT-Extended>=4.7,<5.0

# --- Security middleware ---
Flask-Cors>=5.0,<7.0
Flask-Limiter>=3.9,<4.0

# --- Validation ---
# Pydantic v2 rather than Marshmallow: schemas double as our type
# contract, and ValidationError maps cleanly onto the
# {"errors": {"field": "message"}} envelope the spec requires.
pydantic[email]>=2.9,<3.0

# --- Database driver ---
# psycopg 3 (not psycopg2), used via the postgresql+psycopg:// scheme.
psycopg[binary]>=3.2,<4.0

# --- Config & serving ---
python-dotenv>=1.0,<2.0
gunicorn>=23.0,<24.0

# --- Testing ---
pytest>=8.3,<9.0
