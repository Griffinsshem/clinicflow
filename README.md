# ClinicFlow

Appointment and follow-up management for small clinics.

- `backend/` — Flask + PostgreSQL REST API (`/api/v1`)
- `frontend/` — Next.js App Router + TypeScript + Tailwind

Full documentation to follow.
